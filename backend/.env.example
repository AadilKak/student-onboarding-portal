# Postgres (production / docker-compose). For quick local dev you can instead
# set DATABASE_URL=sqlite:///dev.db
DATABASE_URL=postgresql+psycopg2://sis:sis@localhost:5432/sis
JWT_SECRET_KEY=change-me-in-production
